name: demo
# Loopback mock rig — run tools/mock_pi.py on this machine, then use the experiment UI
# end-to-end without hardware.
data:
  leader_dir: ''
  video_dir: ''
devices: {}
network:
  api_port: 5080
  command_port: 5572
  event_port: 5571
pis:
- devices: []
  ip: 127.0.0.1
  name: mock-pi
  role: leader
  user: vruser
shepherd:
  enabled: false
slack:
  enabled: false
  webhook_url: ''
